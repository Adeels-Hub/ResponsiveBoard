IAM (Identity and Access Management) is AWS's service for **managing users, roles, and permissions.**

✅ Users – Individual accounts with specific access.  
✅ Groups – Collection of users with shared permissions.  
✅ Roles – Temporary access for AWS services (e.g., Lambda, EC2).  
✅ Policies – JSON rules defining what actions/resources are allowed. 🚀